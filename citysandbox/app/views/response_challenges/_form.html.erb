<%= form_for([@challenge, ResponseChallenge.new]) do |f| %>
  <% if @response_challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@response_challenge.errors.count, "error") %> prohibited this response_challenge from being saved:</h2>

      <ul>
      <% @response_challenge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
