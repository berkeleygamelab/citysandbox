<style type="text/css">
html {
        background: url('../images/splash_background.png') no-repeat center center fixed;
        -webkit-background-size: cover;
        -moz-background-size: cover;
        -o-background-size: cover;
        background-size: cover;
}
</style>

<script type="text/javascript">
$.fn.center = function (top_offset) {
	window_width =  $(window).width() > 500 ? $(window).width() : 500;
    this.css("position","absolute");
	this.css("top", ($('.splash_box').position().top + top_offset)  + 'px');
    this.css("left", ((window_width - this.outerWidth()) / 2) + $(window).scrollLeft() + "px");
    return this;
}
$(document).ready(function() {
	center_elements();
	
	$(window).resize(function() {
		center_elements();
	});
	
	function center_elements() {
		window_height =  $(window).height() > 700 ? $(window).height() : 700;
		$('.splash_box').center();
	   	$('.splash_box').css("top", ((window_height - $('.splash_box').outerHeight()) / 2) + "px");
		$('.splash_title').center(20);
		$('.splash_logo').center(150);
		$('.splash_login_box').center(400);
		$('#links').center(550);
		$('#username').center(400 + $('#username').outerHeight());
		$('#password-clear').center(400 + $('#username').outerHeight() * 2 + 15);
		$('#password-password').center(400 + $('#username').outerHeight() * 2 + 15);
	}
	
	$('#username').tbHinter({
		text: 'User ID',
		class: 'gray_text'
	});	
	$('#password-clear').tbHinter({
		text: 'Password',
		class: 'gray_text'
	});
	
	$('#password-clear').show();
	$('#password-password').hide();

	$('#password-clear').focus(function() {
	    $('#password-clear').hide();
	    $('#password-password').show();
	    $('#password-password').focus();
	});
	$('#password-password').blur(function() {
	    if($('#password-password').val() == '') {
	        $('#password-clear').show();
	        $('#password-password').hide();
	    }
	});

 });
</script>
<%= image_tag "../images/splash_box.png", :class=>'splash_box' %>
<%= image_tag "../images/splash_title.png", :class=>'splash_title' %>
<%= image_tag "../images/splash_logo_temp.png", :class=>'splash_logo' %>
<%= image_tag "../images/splash_login_box.png", :class=>'splash_login_box' %>

<input type="text" id="username" size="18"/>
<input id="password-clear" type="text" size="18" class: 'greyText'/>
<input id="password-password" type="password" name="password" size="18" />
<div id="links">
	<%=link_to "Cancel", home_splash_path %>
	<%=link_to "Log In", summary_path %>
</div>